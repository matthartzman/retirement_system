org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Chaquopy needs a specific NDK ABI set; restrict to arm64 + x86_64 (emulator)
# for faster local builds. Add armeabi-v7a/x86 only if a 32-bit device target
# is actually needed (see documentation/ANDROID_MOBILE_ENHANCEMENT_PLAN.md, 3.2).
